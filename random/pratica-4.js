// Calcular o valor de desconto de um determinado produto

const carro = 12000
const desconto = 23/100
const resultado = (carro) * (1 - desconto)

console.log(`"O valor do seu futuro carro com o desconto aplicado será R$ ${resultado}. Qual será a forma de pagamento? "`)